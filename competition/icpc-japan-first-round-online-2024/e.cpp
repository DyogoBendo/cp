#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n;

    while(n){

        vector<int> vc(n);
        for (int i = 0; i < n; i++)
        {
            cin >> vc[i];
        }

        vector<vector<int>> m(n, vector<int>(n));

        int mid = n/2;
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < n; j++)
            {                        
                if(i < mid && j < mid){
                    q = 0;
                }

            }
            
        }
        
        

        cin >> n;
    }
}