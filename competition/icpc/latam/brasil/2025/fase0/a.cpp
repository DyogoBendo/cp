#include <bits/stdc++.h>

using namespace std;

int main(){
    int c, g;
    cin >> c >> g;

    if(c == 1) cout << "vivo e morto";
    else if(g == 0) cout << "morto";
    else if(g == 1) cout << "vivo";
    cout << endl;
}